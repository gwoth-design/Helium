cmake -S . -B build/              #build the build directory. dont need again this project

cmake --build build/              #compiles the code

./build/Debug/heli.exe test.hm    #runs the code with the code as an input